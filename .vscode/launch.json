{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "AccessPath API",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}",
            "args": []
        }
    ]
}
